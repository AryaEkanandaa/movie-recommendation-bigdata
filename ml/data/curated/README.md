# Curated Data

Dataset input yang sudah diperkaya dan siap dipakai untuk finalisasi dataset.

File utama:

- `tmdb_movie_documents.csv`: dokumen teks film, termasuk `movie_document_weighted`.
- `tmdb_movies_model_ready.csv`: metadata film lengkap untuk payload dan scoring.
