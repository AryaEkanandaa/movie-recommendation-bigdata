# Movie Vectors

Folder ini berisi vector/embedding film hasil training Word2Vec.

File utama:

- `movie_vectors_word2vec.parquet`

Dibuat oleh:

```bash
docker compose run --rm ml python ml/scripts/modelling/train_word2vec.py
```
